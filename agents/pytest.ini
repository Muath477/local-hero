[pytest]
testpaths = tests
# `live` tests talk to a real Ollama and can take minutes on CPU — opt in with `pytest -m live`.
addopts = -m "not live" -q
markers =
    live: needs a running Ollama with the configured models pulled (run with `pytest -m live`)
filterwarnings =
    ignore::DeprecationWarning
